# URL pública do backend (Fastify). Usada SÓ no servidor (lib/api.ts é server-only),
# nunca vai pro browser. Não é segredo — é só uma URL.
#
# Local:     http://localhost:3333
# Produção:  https://<seu-back>.up.railway.app  (ou Render/Fly)
BACKEND_URL=http://localhost:3333
